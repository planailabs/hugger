// Default configuration. The popup overrides these via chrome.storage.sync.
// Tests overwrite this file to pre-seed a server URL + token without UI clicks.
self.HUGGER_DEFAULTS = { serverUrl: "http://localhost:7860", token: "" };
